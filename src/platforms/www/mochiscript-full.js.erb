(function (window) {
var log = window.console ? window.console.log : function () { };

var $m  = {
  ROOT: window,
  ADAPTER: {
    out:  log,
    outs: log
  },
  PLATFORM: 'browser'
};

window.$m = $m;

<%= @boot %>
<%= @parser %>
})(window);
